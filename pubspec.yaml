name: pub_api
description: A package to use Pub's API.
version: 0.1.0
homepage: https://github.com/Ampless/pub_api

environment:
  sdk: ^3.0.0

dependencies:
  pub_semver: ^2.0.0
  schttp: ^5.0.0

dev_dependencies:
  crypto: ^3.0.6
  lints: ^5.0.0
  test: ^1.25.0
